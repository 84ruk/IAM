import { ApiClient } from './api'

// Crear una instancia global del cliente API
export const apiClient = new ApiClient()
 
// Exportar también la clase para uso directo si es necesario
export { ApiClient } 