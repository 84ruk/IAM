# ========================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ========================================
# COPIA ESTE ARCHIVO A .env Y CONFIGURA TUS VALORES REALES
# NUNCA COMMITEES EL ARCHIVO .env REAL

# ========================================
# BASE DE DATOS
# ========================================
# URL de conexión a PostgreSQL
# Formato: postgresql://usuario:contraseña@host:puerto/base_datos
DATABASE_URL="postgresql://usuario:contraseña@localhost:5432/iam_db"

# ========================================
# AUTENTICACIÓN JWT - SEGURIDAD MEJORADA
# ========================================
# Clave secreta para firmar tokens JWT (64 caracteres base64)
# Generada con: openssl rand -base64 64
# IMPORTANTE: Cambia esta clave en producción y rótala cada 90 días
JWT_SECRET="ASjgfgRMznJu9kHzbdsoqXXCfh99PTrQEFxO9VgfethW+J6EYmWnFOqgPGI+nzX9qM8Yls1vzCPV66PQB/JFxQ=="

# Tiempo de expiración del token JWT (reducido para mayor seguridad)
JWT_EXPIRES_IN="1d"

# Issuer y Audience para JWT (debe coincidir con la configuración del backend)
# En desarrollo: http://localhost:3001
# En producción: https://tu-backend-url.com
JWT_ISSUER="http://localhost:3001"
JWT_AUDIENCE="http://localhost:3001"

# ========================================
# GOOGLE OAUTH - AUTENTICACIÓN SOCIAL
# ========================================
# Obtener estas credenciales desde: https://console.cloud.google.com/apis/credentials
# 1. Crear proyecto en Google Cloud Console
# 2. Habilitar Google+ API
# 3. Crear credenciales OAuth 2.0
# 4. Configurar URIs autorizados y redirect URIs

# Client ID de Google OAuth
GOOGLE_CLIENT_ID="tu-google-client-id.apps.googleusercontent.com"

# Client Secret de Google OAuth
GOOGLE_CLIENT_SECRET="tu-google-client-secret"

# URL de callback para Google OAuth
# En desarrollo: http://localhost:3001/auth/google/callback
# En producción: https://tu-backend-url.com/auth/google/callback
GOOGLE_CALLBACK_URL="http://localhost:3001/auth/google/callback"

# ========================================
# RATE LIMITING - PROTECCIÓN CONTRA ATAQUES
# ========================================
# Configuración de rate limiting para endpoints críticos
RATE_LIMIT_TTL=60000
RATE_LIMIT_LIMIT=10

# Rate limiting específico para login
LOGIN_RATE_LIMIT_TTL=60000
LOGIN_RATE_LIMIT_LIMIT=5

# Rate limiting para registro
REGISTER_RATE_LIMIT_TTL=300000
REGISTER_RATE_LIMIT_LIMIT=3

# NUEVO: Rate limiting obligatorio en producción
FORCE_RATE_LIMIT=false

# ========================================
# SEGURIDAD AVANZADA
# ========================================
# Habilitar autenticación de dos factores
ENABLE_2FA=false

# Habilitar logs de auditoría (habilitado por defecto)
ENABLE_AUDIT_LOG=true

# Habilitar monitoreo de seguridad
ENABLE_SECURITY_MONITORING=false

# Habilitar testing automatizado de seguridad
ENABLE_AUTOMATED_TESTING=false

# Timeout de sesión en milisegundos (1 hora por defecto)
SESSION_TIMEOUT=3600000

# Máximo número de sesiones concurrentes por usuario
MAX_CONCURRENT_SESSIONS=5

# ========================================
# POLÍTICA DE CONTRASEÑAS
# ========================================
# Longitud mínima de contraseña
PASSWORD_MIN_LENGTH=12

# Requerir mayúsculas
PASSWORD_REQUIRE_UPPERCASE=true

# Requerir minúsculas
PASSWORD_REQUIRE_LOWERCASE=true

# Requerir números
PASSWORD_REQUIRE_NUMBERS=true

# Requerir símbolos
PASSWORD_REQUIRE_SYMBOLS=true

# Prevenir contraseñas comunes
PASSWORD_PREVENT_COMMON=true

# ========================================
# SERVIDOR
# ========================================
# Puerto donde correrá el servidor
PORT=3001

# Entorno de ejecución
NODE_ENV="development"

# ========================================
# CORS (Cross-Origin Resource Sharing)
# ========================================
# URL del frontend para configurar CORS
# En desarrollo: http://localhost:3000
# En producción: https://tu-frontend-url.com
FRONTEND_URL="http://localhost:3000"

# ========================================
# LOGGING Y AUDITORÍA
# ========================================
# Nivel de logging (error, warn, info, debug)
LOG_LEVEL="info"

# Habilitar logging de eventos de autenticación
AUTH_AUDIT_ENABLED=true

# ========================================
# SEGURIDAD ADICIONAL
# ========================================
# Tiempo de tolerancia para sincronización de reloj (segundos)
JWT_CLOCK_TOLERANCE=30

# Habilitar validación estricta de JWT
JWT_STRICT_VALIDATION=true

# ========================================
# PRODUCCIÓN (solo para producción)
# ========================================
# Configuración SSL para producción
SSL_KEY_PATH="/path/to/ssl/key.pem"
SSL_CERT_PATH="/path/to/ssl/cert.pem"

# Configuración de base de datos para producción
PROD_DATABASE_URL="postgresql://prod_user:prod_password@prod_host:5432/prod_db"

# ========================================
# SERVICIOS EXTERNOS (opcional)
# ========================================
# Email service (opcional)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="tu_email@gmail.com"
SMTP_PASS="tu_contraseña_de_aplicacion"

# ========================================
# MONITORING (opcional)
# ========================================
# Sentry para monitoreo de errores
SENTRY_DSN="https://tu-dsn-de-sentry@sentry.io/project"

# ========================================
# NOTAS DE SEGURIDAD JWT
# ========================================
# ✅ CLAIMS ESTÁNDAR IMPLEMENTADOS:
# - iss (issuer): http://localhost:3001 (desarrollo) / https://tu-backend-url.com (producción)
# - aud (audience): http://localhost:3001 (desarrollo) / https://tu-backend-url.com (producción)  
# - iat (issued at): timestamp de emisión
# - exp (expiration): timestamp de expiración
# - jti (JWT ID): identificador único del token
# - sub (subject): ID del usuario
#
# ✅ RATE LIMITING IMPLEMENTADO:
# - Login: 5 intentos por 15 minutos
# - Registro: 3 intentos por 1 hora
# - Password Reset: 3 intentos por 1 hora
# - API General: 100 requests por 15 minutos
# - Admin: 50 requests por 15 minutos
#
# ✅ VALIDACIONES DE SEGURIDAD:
# - Validación de claims requeridos
# - Validación de issuer y audience
# - Validación de expiración automática
# - Clave secreta de 64 caracteres base64
# - Blacklist de tokens revocados
# - Detección de actividad sospechosa
#
# ✅ GOOGLE OAUTH IMPLEMENTADO:
# - Autenticación social con Google
# - Creación automática de usuarios
# - Integración con JWT existente
#
# 🔄 PRÓXIMAS MEJORAS:
# - 2FA (Two-Factor Authentication)
# - Rotación automática de claves
# - Auditoría completa de eventos
# - Monitoreo de seguridad en tiempo real 

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0 