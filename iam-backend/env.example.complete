# ===========================================
# CONFIGURACIÓN CRÍTICA - REQUERIDAS
# ===========================================

# JWT Configuration - CRÍTICO: Debe tener al menos 32 caracteres
JWT_SECRET=tu-super-secreto-jwt-muy-largo-y-seguro-de-al-menos-32-caracteres
JWT_REFRESH_SECRET=tu-super-secreto-refresh-jwt-muy-largo-y-seguro-de-al-menos-32-caracteres
JWT_ISSUER=http://localhost:3001
JWT_AUDIENCE=http://localhost:3001

# Base de Datos - CRÍTICO
DATABASE_URL="postgresql://usuario:password@localhost:5432/iam_db?schema=public"
DIRECT_URL="postgresql://usuario:password@localhost:5432/iam_db?schema=public"

# CORS y Frontend - CRÍTICO
FRONTEND_URL=http://localhost:3000
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ===========================================
# CONFIGURACIÓN OPCIONAL
# ===========================================

# Entorno
NODE_ENV=development
PORT=3001

# JWT (opcionales)
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=7d

# Rate Limiting
RATE_LIMIT_MAX=100

# Google OAuth (opcional - solo si usas login con Google)
GOOGLE_CLIENT_ID=tu-google-client-id
GOOGLE_CLIENT_SECRET=tu-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/auth/google/callback

# Cookies
COOKIE_DOMAIN=localhost

# Logging
LOG_LEVEL=info
JWT_AUDIT_ENABLED=true
JWT_AUDIT_LOG_PATH=./logs/jwt-audit.log

# ===========================================
# CONFIGURACIÓN DE PRODUCCIÓN
# ===========================================
# Para producción, cambia las URLs a HTTPS:
# JWT_ISSUER=https://tu-dominio.com
# JWT_AUDIENCE=https://tu-dominio.com
# FRONTEND_URL=https://tu-frontend.com
# CORS_ORIGINS=https://tu-frontend.com,https://www.tu-frontend.com
# COOKIE_DOMAIN=.tu-dominio.com
# GOOGLE_CALLBACK_URL=https://tu-backend.com/auth/google/callback 